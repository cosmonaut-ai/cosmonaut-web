// Enable SSR and pre-rendering for the landing page so meta tags, headings,
// and structured data are baked into static HTML. This is essential for both
// traditional search crawlers and AI assistants (Claude, Gemini, Perplexity,
// etc.) that do not execute JavaScript when indexing.
export const ssr = true;
export const prerender = true;
